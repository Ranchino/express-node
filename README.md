# A todo app built with Express and Node.js, 
## Uses fetch to make request to REST API.

### To run this app
1. Run "npm install" to install dependencies
2. To start server run "nodemon express-node-api"
